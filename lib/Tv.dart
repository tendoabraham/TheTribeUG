import 'package:flutter/cupertino.dart';

class tribeTv extends StatelessWidget {
  const tribeTv({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
