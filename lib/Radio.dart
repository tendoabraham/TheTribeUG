import 'package:flutter/cupertino.dart';

class tribeRadio extends StatelessWidget {
  const tribeRadio({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
